// Assets
import $ from "jquery";

// Components

const checkinRanking = () => {
  const ref = $("._checkin-ranking");

  for (let c = 0; c < ref.length; c++) {
    const component = $(ref[c]);
  }
};
//checkinRanking();
