// Assets
import $ from "jquery";

// Components
import "../../../../components/Profile/Menu";

const MainSidebarMenu = () => {
  const ref = $("._user-sidebar-menu");

  for (let c = 0; c < ref.length; c++) {
    const component = $(ref[c]);
  }
};
//MainSidebarMenu();
